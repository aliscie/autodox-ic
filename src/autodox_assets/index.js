import {autodox} from "../declarations/autodox";


// export async function test_canister(name) {
//     return await autodox.greet(name)
// }

export function test_canister(name) {
    return name + " xxx"
}
